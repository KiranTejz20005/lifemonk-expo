/**
 * mapping-control  —  admin/src/index.tsx
 *
 * Re-exports the admin entry-point so Strapi can pick it up
 * from the standard plugin discovery path.
 */

export { default } from '../../strapi-admin';
